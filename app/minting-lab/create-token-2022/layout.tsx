import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
    title: 'Create SPL Token-2022 | Bundler Space',
    description: 'Create your own SPL Token-2022 with extensions on Solana with Bundler Space',
};

export default function CreateToken2022Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
} 