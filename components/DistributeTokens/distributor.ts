// import { PublicKey } from "@metaplex-foundation/js";
// import { connection } from "../removeLiquidity/config";

// export async function TokenDisperser(wallets: [string], signAllTransactions: any, fundingWallet: PublicKey) {
    // let tokenAmount = await connection.getTokenAccountBalance(fundingWallet);
    // Split walletAddresses of string to convert them to PublicKey
    // const walletAddressesArray = wallets.map(wallet => new PublicKey(wallet));
// }
export {}


